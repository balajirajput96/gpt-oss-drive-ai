# Reel 0006 Existing-Package Reconciliation

## Deterministic identity

| Field | Verified canonical value |
|---|---|
| Reel ID | `0006` |
| Registry tuple | `MND` / `L01` / `Q06` |
| Registry question | किस बात पर शोध सहमत नहीं है? |
| Canonical title | Predictive processing पर शोध कहाँ असहमत है? |
| Canonical Drive folder | `REEL-0006` (`14Ud3qCRv27UWcli5x01JR9zoQaXRf6zU`) |
| Adopted canonical MP4 | `REEL-0006_reconciled_MND-L01-Q06.mp4` (`1T4LlRIQlmbaUwx5rP2AQHQeN8vRf1HLj`) |
| Adopted manifest | `REEL-0006_reconciled_manifest.json` (`1bT1JfzoqKBpXJtq0Z2PCuZLHoFC_zrwW`) |

## Why reuse is permitted

The reconciled manifest explicitly records the same pillar, lens, question code, and question as the deterministic registry. It also separates theory from empirical evidence, records disagreement and counterevidence, includes a 62.666-second 720×1280 H.264/AAC output, and records a post-upload re-fetch checksum. This package is therefore adopted rather than re-rendered, preventing an unnecessary duplicate Reel 0006.

The current Drive re-fetch confirmed the canonical video’s parent folder, MIME type, 720×1280 metadata, duration of 62.666 seconds, size of 13,684,568 bytes, and MD5 `58abc57918299e4f293f3797b5101283`. The reconciled QC record reports all required artifacts non-trashed and the package SHA-256 `7a1bb16ef0e6fd3f0f2e420581f99c413a79cb48a776db7fbc97361482c353ba`.

## Preservation rule

The same folder contains an earlier conflicting set of `REEL-0006` artifacts, including video `1rjeTbIVEf7x69hxBVxTySLyoizIi7qtR`. Those artifacts remain untouched and non-canonical. The reconciled package’s own manifest records that no prior artifact was overwritten. No content is deleted, renamed, or transferred between folders in this reconciliation.
